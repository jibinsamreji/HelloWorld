course = 'I am learning Python ! '
print(len(course))
# len() is a general purpose function and can be used to find the length
# len() can be used to limit characters in an input field

print(course.upper())  # functions/methods associated with stringd

print(course.find('P'))  # find() to find a char or word and returns index at which its found

print('Python' in course)  # 'in' to find a word or char and returns a Boolean

print(course.replace('I am', 'Jibin is'))
